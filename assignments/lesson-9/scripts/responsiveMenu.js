/* Toggle between adding and removing the "responsive" class to topnav when the user clicks on the icon */
}function myFunction() {
   document.getElementById("primaryNavigation").innerHTML = "Paragraph changed.";
    if (x.className === "show") {
        x.className = "hide";
    } else {
        x.className = "show";
    }